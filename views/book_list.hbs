
<h1>{{title}}</h1>


<ul>
    {{#each (sort_az book_list)}}
    <li>
        <a href={{this.url}}>{{this.title}}</a> ({{this.author.name}})
    </li>
    
    {{else}}
    
    <li>There are no books.</li>

    {{/each}}

</ul>
